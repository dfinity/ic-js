name: Publish Docs

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref (tag, branch, or commit SHA) to build docs from'
        required: true
        type: string
  workflow_call:
    inputs:
      ref:
        description: 'Git ref (tag, branch, or commit SHA) to build docs from'
        required: true
        type: string

jobs:
  publish-docs:
    runs-on: ubuntu-latest
    environment: release
    steps:
      - name: Create GitHub App Token
        uses: actions/create-github-app-token@67018539274d69449ef7c02e8e71183d1719ab42 # v2.1.4
        id: generate_token
        with:
          app-id: ${{ vars.PR_AUTOMATION_BOT_PUBLIC_APP_ID }}
          private-key: ${{ secrets.PR_AUTOMATION_BOT_PUBLIC_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: icp-js-sdk-docs

      - name: Checkout source at ref
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          ref: ${{ inputs.ref }}

      - name: Prepare
        uses: ./.github/actions/prepare

      - uses: dfinity/ci-tools/actions/extract-version@main
        id: extract-version
        with:
            file: packages/canisters/package.json

      - name: Prepare version
        id: ver
        run: |
          echo "major_version=v${{ steps.extract-version.outputs.major }}" >> $GITHUB_OUTPUT
          echo "major_minor_patch_version=v${{ steps.extract-version.outputs.major }}.${{ steps.extract-version.outputs.minor }}.${{ steps.extract-version.outputs.patch }}" >> $GITHUB_OUTPUT

      - name: Build packages
        run: npm run build

      - name: Build docs for version latest
        env:
          DOCS_VERSION: ${{ steps.ver.outputs.major_version }}
        run: npm run docs

      - name: Build docs for version latest
        env:
          DOCS_VERSION: latest
        run: npm run docs

      - name: Checkout icp-pages branch
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          ref: icp-pages
          path: icp-pages

      - name: Assemble docs
        uses: dfinity/ci-tools/actions/assemble-docs@main
        with:
          assets_dir: 'docs/dist/${{ steps.ver.outputs.major_version }}'
          version: ${{ steps.ver.outputs.major_version }}
          target_dir: 'icp-pages'
          version_label: ${{ steps.ver.outputs.major_version }}
          version_in_title: ${{ steps.ver.outputs.major_minor_patch_version }}

      - name: Assemble docs
        uses: dfinity/ci-tools/actions/assemble-docs@main
        with:
          assets_dir: 'docs/dist/latest'
          version: 'latest'
          target_dir: 'icp-pages'
          version_label: 'Latest (${{ steps.ver.outputs.major_minor_patch_version }})'

      - name: Submit Documentation
        uses: dfinity/ci-tools/actions/submit-docs@main
        with:
          destination_repo: 'dfinity/icp-js-sdk-docs'
          token: ${{ steps.generate_token.outputs.token }}
          target_dir: 'icp-pages'
